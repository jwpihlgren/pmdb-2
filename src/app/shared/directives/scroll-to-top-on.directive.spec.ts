import { ScrollToTopOnDirective } from './scroll-to-top-on.directive';

describe('ScrollToTopOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTopOnDirective();
    expect(directive).toBeTruthy();
  });
});
