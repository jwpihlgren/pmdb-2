import { AppBackButtonDirective } from './app-back-button.directive';

describe('AppBackButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBackButtonDirective();
    expect(directive).toBeTruthy();
  });
});
