import { AppEventTriggerDirective } from './app-event-trigger.directive';

describe('AppEventTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new AppEventTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
